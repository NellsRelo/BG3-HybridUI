<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:System="clr-namespace:System;assembly=mscorlib"
					xmlns:ls="clr-namespace:ls;assembly=Code"
					xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

      <ControlTemplate x:Key="originHeaderTemplate">
          <DockPanel>
              <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
                  <Image Source="{StaticResource CarouselDecoration}" Stretch="None" Margin="25,98,0,-148" Height="108" HorizontalAlignment="Center">
                      <Image.RenderTransform>
                          <ScaleTransform ScaleY="-1"></ScaleTransform>
                      </Image.RenderTransform>
                  </Image>
                  <TextBlock ls:TextBlockFormatter.SourceText="{Binding SelectedOrigin.Name}" Style="{StaticResource PanelHeaderText}" TextAlignment="Center" Margin="0,-10,0,0"/>
              </StackPanel>
        </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="originDescriptionTemplate">
      <DockPanel>
          <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
              <ContentControl Template="{StaticResource PanelSideDescription}" VerticalAlignment="Top">
                  <TextBlock ls:TextBlockFormatter.SourceText="{Binding SelectedOrigin.Description}" Style="{StaticResource CarouselSideDescriptionText}"/>
              </ContentControl>
          </StackPanel>
          </DockPanel>
    </ControlTemplate>

</ResourceDictionary>