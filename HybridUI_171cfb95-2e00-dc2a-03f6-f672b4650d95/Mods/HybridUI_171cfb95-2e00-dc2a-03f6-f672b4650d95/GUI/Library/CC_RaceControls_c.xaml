<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:System="clr-namespace:System;assembly=mscorlib"
					xmlns:ls="clr-namespace:ls;assembly=Code"
					xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

      <ControlTemplate x:Key="raceSelectionTemplate">
          <DockPanel>

              <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
                  <ContentControl Template="{StaticResource HUI_SelectorHeader}" Grid.Row="0" />

                  <ContentControl Template="{StaticResource HUI_CarouselHeaderDecorator}" Grid.Row="1" Margin="0,10,0,0">
                      <ListBox x:Name="RaceCarousel" Template="{StaticResource RaceCarouselWithUIAccept}" 
                              ItemsSource="{Binding SelectableRaces}" SelectedItem="{Binding SelectedRace}">
                          <ListBox.Style>
                              <Style TargetType="ListBox">
                                  <Style.Triggers>
                                      <DataTrigger Binding="{Binding ElementName=raceListBox, Path=Visibility}" Value="Visible">
                                          <Setter Property="IsEnabled" Value="False"/>
                                      </DataTrigger>
                                  </Style.Triggers>
                              </Style>
                          </ListBox.Style>
                      </ListBox>
                    </ContentControl>
              </StackPanel>

              <ScrollViewer x:Name="raceScrollViewer" Style="{StaticResource gameplayPanelScrollViewerStyle}"
                            VerticalScrollBarVisibility="Auto" MaxHeight="800" Height="Auto" Grid.Row="2"
                            DockPanel.Dock="Top" Margin="0,20,0,0">

                <StackPanel x:Name="listStackPanel">
                    <b:Interaction.Triggers>
                        <b:PropertyChangedTrigger Binding="{Binding ElementName=raceListBox, Path=SelectedItem}">
                            <b:ChangePropertyAction TargetName="raceListBox" PropertyName="Visibility" Value="Collapsed"/>
                            <b:ChangePropertyAction TargetName="subRaceListBox" PropertyName="Visibility" Value="Visible"/>
                            <ls:SetMoveFocusAction TargetName="CharacterCreation_c" FocusElement="{Binding ElementName=RaceCarousel}"/>
                        </b:PropertyChangedTrigger>
                    </b:Interaction.Triggers>
                    
                <!-- SubRace ListBox - default visible -->
                <ListBox x:Name="subRaceListBox" ItemTemplate="{StaticResource CustomIconTemplate}"
                        ItemsSource="{Binding SelectableSubRaces}" SelectedItem="{Binding SelectedSubRace}"
                        ItemContainerStyle="{StaticResource CCIconTextListBoxItemStyle}"
                        Style="{StaticResource gameplayIconGridStyle}">
                    <ListBox.Resources>
                        <Style x:Key="CustomIconStyle" BasedOn="{StaticResource RaceIconStyle}" TargetType="Rectangle"/>
                    </ListBox.Resources>
                </ListBox>
                
                <!-- Race ListBox - hidden by default -->
                <ListBox x:Name="raceListBox" ItemTemplate="{StaticResource CustomIconTemplate}"
                        ItemsSource="{Binding SelectableRaces}" SelectedItem="{Binding SelectedRace}"
                        ItemContainerStyle="{StaticResource CCIconTextListBoxItemStyle}"
                        Style="{StaticResource gameplayIconGridStyle}"
                        IsSynchronizedWithCurrentItem="False"
                        Visibility="Collapsed">
                    <ListBox.Resources>
                        <Style x:Key="CustomIconStyle" BasedOn="{StaticResource RaceIconStyle}" TargetType="Rectangle"/>
                    </ListBox.Resources>
                    <b:Interaction.Triggers>
                        <b:PropertyChangedTrigger Binding="{Binding ElementName=raceListBox, Path=Visibility}">
                            <b:Interaction.Behaviors>
                                <b:ConditionBehavior>
                                    <b:ConditionalExpression>
                                        <b:ComparisonCondition LeftOperand="{Binding ElementName=raceListBox, Path=Visibility}" Operator="Equal" RightOperand="Visible"/>
                                    </b:ConditionalExpression>
                                </b:ConditionBehavior>
                            </b:Interaction.Behaviors>
                            <b:ChangePropertyAction TargetName="raceListBox" PropertyName="SelectedItem" Value="{Binding SelectedRace}"/>
                        </b:PropertyChangedTrigger>
                    </b:Interaction.Triggers>
                </ListBox>
                </StackPanel>
              </ScrollViewer>
              
              <!-- Collapse Race ListBox -->
              <ls:LSInputBinding BoundEvent="UICancel" IsEnabled="{Binding ElementName=raceListBox, Path=IsVisible}">
                  <b:Interaction.Triggers>
                      <b:EventTrigger EventName="LSInputBindingReleased">
                          <b:ChangePropertyAction TargetName="raceListBox" PropertyName="Visibility" Value="Collapsed"/>
                          <b:ChangePropertyAction TargetName="subRaceListBox" PropertyName="Visibility" Value="Visible"/>
                          <ls:SetMoveFocusAction TargetName="CharacterCreation_c" FocusElement="{Binding ElementName=RaceCarousel}"/>
                          <ls:LSPlaySound Sound="UI_DefaultButton_Click"/>
                      </b:EventTrigger>
                  </b:Interaction.Triggers>
              </ls:LSInputBinding>
              
              <!-- Auto-collapse Race ListBox when SelectedRace changes -->
              <b:Interaction.Triggers>
                  <b:PropertyChangedTrigger Binding="{Binding SelectedRace}">
                      <b:Interaction.Behaviors>
                          <b:ConditionBehavior>
                              <b:ConditionalExpression>
                                  <b:ComparisonCondition LeftOperand="{Binding ElementName=raceListBox, Path=Visibility}" RightOperand="Visible"/>
                              </b:ConditionalExpression>
                          </b:ConditionBehavior>
                      </b:Interaction.Behaviors>
                      <b:ChangePropertyAction TargetName="raceListBox" PropertyName="Visibility" Value="Collapsed"/>
                      <b:ChangePropertyAction TargetName="subRaceListBox" PropertyName="Visibility" Value="Visible"/>
                      <ls:SetMoveFocusAction TargetName="CharacterCreation_c" FocusElement="{Binding ElementName=RaceCarousel}"/>
                  </b:PropertyChangedTrigger>
              </b:Interaction.Triggers>
              
          </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="raceDescriptionTemplate">
          <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
              <ContentControl Template="{StaticResource PanelSideDescription}" VerticalAlignment="Top">
                <StackPanel>  
                <TextBlock x:Name="SelectionDescriptionRace" ls:TextBlockFormatter.SourceText="{Binding SelectedRace.Description}" Style="{StaticResource CarouselSideDescriptionTextSplitScreen}"/>
                <TextBlock x:Name="SelectionDescription" ls:TextBlockFormatter.SourceText="{Binding SelectedSubRace.Description}" 
                            Style="{StaticResource CarouselSideDescriptionTextSplitScreen}"
                            Visibility="{Binding Path=SelectableSubRaces.Count, Converter={StaticResource CountToVisibilityConverter}}" />
                  </StackPanel>
              </ContentControl>
          </StackPanel>

        <ControlTemplate.Triggers>
            <!-- Split Screen variation -->
            <DataTrigger Binding="{Binding Layout}" Value="Common">
                <Setter TargetName="SelectionDescriptionRace" Property="Style" Value="{StaticResource CarouselSideDescriptionText}"/>
                <Setter TargetName="SelectionDescription" Property="Style" Value="{StaticResource CarouselSideDescriptionText}"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
  </ControlTemplate>

    <ControlTemplate x:Key="raceProgressionsTemplate">
          <Grid HorizontalAlignment="Left">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
              <StackPanel Grid.Row="0">
                  <Image Source="{StaticResource CarouselDecoration}" Stretch="None" Margin="0,-30,0,0" Height="108" HorizontalAlignment="Center" />
                  <TextBlock Text="{Binding Source='h230f60c6g161fg481bgbe52g42bc264618dd', Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource FeatureListHeaderText}"/>
              </StackPanel>
              <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}" VerticalScrollBarVisibility="Auto"
                            Grid.Row="1" Margin="0,-20,0,0">
                  <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">

                      <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=RaceProgressions}" ItemTemplate="{StaticResource progressionSpellsTemplate}" Visibility="{Binding FilteredItems.Count, ElementName=RaceProgressions, Converter={StaticResource CountToVisibilityConverter}}"/>
                      <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=SubRaceProgressions}" ItemTemplate="{StaticResource progressionSpellsTemplate}" Visibility="{Binding FilteredItems.Count, ElementName=SubRaceProgressions, Converter={StaticResource CountToVisibilityConverter}}"/>

                      <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=RaceSpellSelectors}" ItemTemplate="{StaticResource progressionSpellSelectors}" Visibility="{Binding FilteredItems.Count, ElementName=RaceSpellSelectors, Converter={StaticResource CountToVisibilityConverter}}"/>
                      <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=SubRaceSpellSelectors}" ItemTemplate="{StaticResource progressionSpellSelectors}" Visibility="{Binding FilteredItems.Count, ElementName=SubRaceSpellSelectors, Converter={StaticResource CountToVisibilityConverter}}"/>
                      <ItemsControl x:Name="racePassives" ItemsSource="{Binding FilteredItems, ElementName=RacePassiveFeatures}" ItemTemplate="{StaticResource ProgressionSpellsTemplate}" Visibility="{Binding FilteredItems.Count, ElementName=RacePassiveFeatures, Converter={StaticResource CountToVisibilityConverter}}" Margin="0,20,0,0"/>
                      <ItemsControl x:Name="subRacePassives" ItemsSource="{Binding FilteredItems, ElementName=SubRacePassiveFeatures}" ItemTemplate="{StaticResource ProgressionSpellsTemplate}" Visibility="{Binding FilteredItems.Count, ElementName=SubRacePassiveFeatures, Converter={StaticResource CountToVisibilityConverter}}" Margin="0,20,0,0"/>


                      <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.RaceProgression}" HorizontalAlignment="Center"/>
                      <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.SubRaceProgression}" HorizontalAlignment="Center"/>

                  </StackPanel>
                </ScrollViewer>
              </Grid>
    </ControlTemplate>


    <ControlTemplate x:Key="HUI_LevelUpRaceHeader">
        <DockPanel>
            <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
                <ContentControl Template="{StaticResource HUI_SelectorHeader}">
                    <TextBlock ls:TextBlockFormatter.SourceText="{Binding DummyCharacter.Stats.Race.DisplayName}" Style="{StaticResource PanelHeaderText}" TextAlignment="Center"/>
                </ContentControl>
                    <TextBlock ls:TextBlockFormatter.SourceText="{Binding DummyCharacter.Stats.Race.Description}" Style="{StaticResource PanelDescriptionText}"  FontSize="{StaticResource ScaledDefaultFontSize}" Foreground="{StaticResource CCTextNormal}" Margin="0,5,0,0"/>

            </StackPanel>
        </DockPanel>
    </ControlTemplate>


    <ControlTemplate x:Key="HUI_HenchRaceHeader">
        <DockPanel>
            <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
                <ContentControl Template="{StaticResource HUI_SelectorHeader}">
                    <TextBlock Text="{Binding Source='he1ff355fg8a83g4370g9178ga6d9364f4290', Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource PanelHeaderText}"/>
                </ContentControl>
                    <TextBlock ls:TextBlockFormatter.SourceText="{Binding DummyCharacter.Stats.Race.Description}" Style="{StaticResource PanelDescriptionText}"  FontSize="{StaticResource ScaledDefaultFontSize}" Foreground="{StaticResource CCTextNormal}" Margin="0,5,0,0"/>

            </StackPanel>
        </DockPanel>
    </ControlTemplate>
    <ControlTemplate x:Key="HUI_HenchSubRaceHeader">
        <DockPanel>
            <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
                <ContentControl Template="{StaticResource HUI_SelectorHeader}">
                    <TextBlock Text="{Binding Source='h09806be4gc585g4b00gbd9dg59502d635bd9', Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource PanelHeaderText}"/>
                </ContentControl>
                    <TextBlock ls:TextBlockFormatter.SourceText="{Binding DummyCharacter.Stats.Race.Description}" Style="{StaticResource PanelDescriptionText}"  FontSize="{StaticResource ScaledDefaultFontSize}" Foreground="{StaticResource CCTextNormal}" Margin="0,5,0,0"/>

            </StackPanel>
        </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="HUI_HenchRaceProgressions">
        <DockPanel>
            <b:Interaction.Behaviors>
                <ls:CollectionFilterBehavior x:Name="RacePassiveSelectors" ItemsSource="{Binding RaceProgressionDetails.NotSubPassiveSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="RaceSpellSelectors" ItemsSource="{Binding RaceProgressionDetails.NotSubSpellSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="RaceProgressions" ItemsSource="{Binding RaceProgressionDetails.Progressions}" Predicate="{Binding IsNotSubProgressionPredicate}"/>
            </b:Interaction.Behaviors>
            
            <Control Template="{StaticResource HUI_SelectorClosingTemplate}" DockPanel.Dock="Top" Margin="0,50,0,0"/>
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}" VerticalScrollBarVisibility="Auto"
                          Grid.Row="1" Margin="0,50,0,0" Height="1200">
                <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">

                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=RacePassiveSelectors}" ItemTemplate="{StaticResource progressionPassiveSelectors}" ItemContainerStyle="{StaticResource UpdateStateItemStyle}"/>
                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=RaceSpellSelectors}" ItemTemplate="{StaticResource progressionSpellSelectors}" Visibility="{Binding FilteredItems.Count, ElementName=RaceSpellSelectors, Converter={StaticResource CountToVisibilityConverter}}"/>

                    <ItemsControl x:Name="featureSpells" ItemsSource="{Binding FilteredItems, ElementName=RaceProgressions}" ItemTemplate="{StaticResource progressionSpellsTemplate}"/>

                    <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.RaceProgression}" HorizontalAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="HUI_HenchSubRaceProgressions">
        <DockPanel>
            <b:Interaction.Behaviors>
                <ls:CollectionFilterBehavior x:Name="SubRacePassiveSelectors" ItemsSource="{Binding RaceProgressionDetails.SubPassiveSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="SubRaceSpellSelectors" ItemsSource="{Binding RaceProgressionDetails.SubSpellSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="SubRaceProgressions" ItemsSource="{Binding RaceProgressionDetails.Progressions}" Predicate="{Binding IsSubProgressionPredicate}"/>
            </b:Interaction.Behaviors>
            
            <Control Template="{StaticResource HUI_SelectorClosingTemplate}" DockPanel.Dock="Top" Margin="0,50,0,0"/>
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}" VerticalScrollBarVisibility="Auto"
                          Grid.Row="1" Margin="0,50,0,0" Height="1200">
                <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">

                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=SubRacePassiveSelectors}" ItemTemplate="{StaticResource progressionPassiveSelectors}" ItemContainerStyle="{StaticResource UpdateStateItemStyle}"/>
                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=SubRaceSpellSelectors}" ItemTemplate="{StaticResource progressionSpellSelectors}" Visibility="{Binding FilteredItems.Count, ElementName=SubRaceSpellSelectors, Converter={StaticResource CountToVisibilityConverter}}"/>

                    <ItemsControl x:Name="subRaceFeatureSpells" ItemsSource="{Binding FilteredItems, ElementName=SubRaceProgressions}" ItemTemplate="{StaticResource progressionSpellsTemplate}" Visibility="{Binding FilteredItems.Count, ElementName=SubRaceProgressions, Converter={StaticResource CountToVisibilityConverter}}"/>

                    <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.SubRaceProgression}" HorizontalAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="HUI_LevelUpRaceProgressions">
        <DockPanel>
            <b:Interaction.Behaviors>
                <ls:CollectionFilterBehavior x:Name="RacePassiveSelectors" ItemsSource="{Binding RaceProgressionDetails.NotSubPassiveSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="SubRacePassiveSelectors" ItemsSource="{Binding RaceProgressionDetails.SubPassiveSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="RaceSpellSelectors" ItemsSource="{Binding RaceProgressionDetails.NotSubSpellSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="SubRaceSpellSelectors" ItemsSource="{Binding RaceProgressionDetails.SubSpellSelectors}"/>
                <ls:CollectionFilterBehavior x:Name="RaceProgressions" ItemsSource="{Binding RaceProgressionDetails.Progressions}" Predicate="{Binding IsNotSubProgressionPredicate}"/>
                <ls:CollectionFilterBehavior x:Name="SubRaceProgressions" ItemsSource="{Binding RaceProgressionDetails.Progressions}" Predicate="{Binding IsSubProgressionPredicate}"/>
            </b:Interaction.Behaviors>
            
            <Control Template="{StaticResource HUI_SelectorClosingTemplate}" DockPanel.Dock="Top" Margin="0,50,0,0"/>
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}" VerticalScrollBarVisibility="Auto"
                          Grid.Row="1" Margin="0,50,0,0" Height="1200">
                <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">

                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=RacePassiveSelectors}" ItemTemplate="{StaticResource progressionPassiveSelectors}" ItemContainerStyle="{StaticResource UpdateStateItemStyle}"/>
                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=SubRacePassiveSelectors}" ItemTemplate="{StaticResource progressionPassiveSelectors}" ItemContainerStyle="{StaticResource UpdateStateItemStyle}"/>
                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=RaceSpellSelectors}" ItemTemplate="{StaticResource progressionSpellSelectors}" Visibility="{Binding FilteredItems.Count, ElementName=RaceSpellSelectors, Converter={StaticResource CountToVisibilityConverter}}"/>
                    <ItemsControl ItemsSource="{Binding FilteredItems, ElementName=SubRaceSpellSelectors}" ItemTemplate="{StaticResource progressionSpellSelectors}" Visibility="{Binding FilteredItems.Count, ElementName=SubRaceSpellSelectors, Converter={StaticResource CountToVisibilityConverter}}"/>

                    <ItemsControl x:Name="featureSpells" ItemsSource="{Binding FilteredItems, ElementName=RaceProgressions}" ItemTemplate="{StaticResource progressionSpellsTemplate}"/>

                    <ItemsControl x:Name="subRaceFeatureSpells" ItemsSource="{Binding FilteredItems, ElementName=SubRaceProgressions}" ItemTemplate="{StaticResource progressionSpellsTemplate}" Visibility="{Binding FilteredItems.Count, ElementName=SubRaceProgressions, Converter={StaticResource CountToVisibilityConverter}}"/>

                    <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.RaceProgression}" HorizontalAlignment="Center"/>

                    <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.SubRaceProgression}" HorizontalAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </ControlTemplate>


</ResourceDictionary>