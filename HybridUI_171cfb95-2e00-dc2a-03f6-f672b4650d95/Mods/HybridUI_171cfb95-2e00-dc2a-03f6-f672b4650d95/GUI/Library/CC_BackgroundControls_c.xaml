<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:System="clr-namespace:System;assembly=mscorlib"
					xmlns:ls="clr-namespace:ls;assembly=Code"
					xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

      <ControlTemplate x:Key="backgroundSelectionTemplate">
        <DockPanel>
          <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
            <ContentControl Template="{StaticResource HUI_SelectorHeader}">
                <TextBlock ls:TextBlockFormatter.SourceText="{Binding SelectedBackground.Name}" Style="{StaticResource PanelHeaderText}" TextAlignment="Center"/>
            </ContentControl>
        </StackPanel>

        <StackPanel x:Name="selectedBackgroundDescription" Orientation="Horizontal" DockPanel.Dock="Top">
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}"
                          VerticalScrollBarVisibility="Auto" MaxHeight="800" Height="Auto"
                          DockPanel.Dock="Top" Margin="0,20,0,0">
                <ListBox x:Name="backgroundListBox" ItemTemplate="{StaticResource HUI_CCIconGridTemplate}"
                        ItemsSource="{Binding SelectableBackgrounds}" SelectedItem="{Binding SelectedBackground}"
                        ItemContainerStyle="{StaticResource CCIconTextListBoxItemStyle}" Style="{StaticResource gameplayIconGridStyle}">
                    <ListBox.Resources>
                        <Style x:Key="CustomIconStyle" BasedOn="{StaticResource BackgroundIconStyle}" TargetType="Rectangle"/>
                    </ListBox.Resources>
                </ListBox>
            </ScrollViewer>
        </StackPanel>
        </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="backgroundProgressionsTemplate">
        <Grid HorizontalAlignment="Left">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <Image Source="{StaticResource CarouselDecoration}" Stretch="None" Margin="0,-30,0,0" Height="108" HorizontalAlignment="Center" />
                <TextBlock ls:TextBlockFormatter.SourceText="{Binding Source='h230f60c6g161fg481bgbe52g42bc264618dd', Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource FeatureListHeaderText}"/>
            </StackPanel>
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}" VerticalScrollBarVisibility="Auto"  Grid.Row="1" Margin="0,-20,0,0">
                <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">
                  <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.BackgroundProgression}" HorizontalAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="backgroundDescriptionTemplate">
          <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
              <ContentControl Template="{StaticResource PanelSideDescription}" VerticalAlignment="Top">
                <StackPanel>  
                <TextBlock x:Name="SelectionDescriptionBackground" ls:TextBlockFormatter.SourceText="{Binding SelectedBackground.Description}" Style="{StaticResource CarouselSideDescriptionTextSplitScreen}"/>
                  </StackPanel>
              </ContentControl>
          </StackPanel>

        <ControlTemplate.Triggers>
            <!-- Split Screen variation -->
            <DataTrigger Binding="{Binding Layout}" Value="Common">
                <Setter TargetName="SelectionDescriptionBackground" Property="Style" Value="{StaticResource CarouselSideDescriptionText}"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
  </ControlTemplate>

    <ControlTemplate x:Key="backgroundProgressionsTemplateSimplified">
          <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">
              <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.BackgroundProgression}" HorizontalAlignment="Center"/>
          </StackPanel>
    </ControlTemplate>

      
      <ControlTemplate x:Key="BackgroundSelectorTemplate">
          <DockPanel>
              <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                  </Grid.RowDefinitions>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>

                  <b:Interaction.Triggers>
                      <!-- Auto-close any open grids when this template loads -->
                      <b:EventTrigger EventName="Loaded">
                          <b:ChangePropertyAction TargetName="CharacterCreation_c" PropertyName="Tag" Value="HideExpandedGrid"/>
                          <!-- Set initial focus to the name input -->
                          <ls:SetMoveFocusAction TargetName="CharacterCreation_c" FocusElement="{Binding ElementName=nameInput}"/>
                      </b:EventTrigger>
                  </b:Interaction.Triggers>

                  <Control Template="{StaticResource setGameplayCameraOffsets}"/>

                  <ContentControl x:Name="ClassOpeningDecorator" Template="{StaticResource HUI_SelectorHeader}" Grid.Row="0"/>
                  <ContentControl Template="{StaticResource HUI_CarouselHeaderDecorator}" Grid.Row="1">
                      <TextBlock ls:TextBlockFormatter.SourceText="{Binding SelectedBackground.Name}" Style="{StaticResource PanelHeaderText}" HorizontalAlignment="Center"  Height="100" Margin="0,-10,0,10"/>
                  </ContentControl>
                  <!-- Background Selection ScrollViewer -->
                  <ScrollViewer x:Name="backgroundSelectionScrollViewer" Grid.Row="2" DockPanel.Dock="Top" Style="{StaticResource gameplayPanelScrollViewerStyle}" 
                                VerticalScrollBarVisibility="Auto" Margin="0,12,0,0" MaxHeight="800" Height="Auto">

                      <ListBox x:Name="backgroundListBox" 
                              ItemTemplate="{StaticResource HUI_CCIconGridTemplate}"
                              ItemsSource="{Binding SelectableBackgrounds}" 
                              SelectedItem="{Binding SelectedBackground}"
                              ItemContainerStyle="{StaticResource CCIconTextListBoxItemStyle}"
                              Style="{StaticResource gameplayIconGridStyle}">
                          <ListBox.Resources>
                              <Style x:Key="CustomIconStyle" BasedOn="{StaticResource BackgroundIconStyle}" TargetType="Rectangle"/>
                          </ListBox.Resources>
                      </ListBox>
                  </ScrollViewer>

                  <!-- Progressions Header -->
                  <StackPanel x:Name="ProgressionsHeader" Grid.Row="3" Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left">
                      <StackPanel.Resources>
                          <Storyboard x:Key="SlideProgressionsHeaderAnimation">
                              <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                               Duration="0:0:0.5" 
                                               From="-60" 
                                               To="0">
                                  <DoubleAnimation.EasingFunction>
                                      <QuadraticEase EasingMode="EaseOut"/>
                                  </DoubleAnimation.EasingFunction>
                              </DoubleAnimation>
                          </Storyboard>
                      </StackPanel.Resources>
                      
                      <b:Interaction.Triggers>
                          <!-- Animate when background collection changes -->
                          <b:PropertyChangedTrigger Binding="{Binding SelectableBackgrounds.Count}">
                              <b:ControlStoryboardAction ControlStoryboardOption="Play" Storyboard="{StaticResource SlideProgressionsHeaderAnimation}"/>
                          </b:PropertyChangedTrigger>
                      </b:Interaction.Triggers>
                      
                      <StackPanel.RenderTransform>
                          <TranslateTransform/>
                      </StackPanel.RenderTransform>
                      
                      <Image Source="{StaticResource CarouselDecoration}" Stretch="None" Margin="25,0,0,0" Height="108" HorizontalAlignment="Center" />
                      <TextBlock Text="{Binding Source='h230f60c6g161fg481bgbe52g42bc264618dd', Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource FeatureListHeaderText}" HorizontalAlignment="Center"/>
                  </StackPanel>

                  <!-- Progressions ScrollViewer -->
                  <ScrollViewer x:Name="progressionsScrollViewer" Grid.Row="4" DockPanel.Dock="Top" Style="{StaticResource gameplayPanelScrollViewerStyle}" 
                                VerticalScrollBarVisibility="Auto" Margin="0,-20,0,0">

                      <Control Template="{StaticResource backgroundProgressionsTemplateSimplified}"/>
                  </ScrollViewer>

                  <Control Template="{StaticResource HUI_DescriptionPanelTemplate}" Tag="{Binding SelectedBackground.Description}" Grid.Column="1" Grid.RowSpan="4" DockPanel.Dock="Top"  Margin="-175,200,0,0"/>

              </Grid>
          </DockPanel>
      </ControlTemplate>

</ResourceDictionary>